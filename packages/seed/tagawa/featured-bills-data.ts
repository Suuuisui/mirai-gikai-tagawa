/**
 * トップページ「注目の議案」の手動設定ファイル
 *
 * ここに載せた議案がトップページの注目セクションに表示される。
 * AI選定ではなく運営による人手のキュレーション（事実として特筆性が
 * 高いもの: 決算不認定・否決など議決が割れた案件、当初予算 等）。
 *
 * ## 書き方
 * - キーは `会期キー:議案番号ラベル`（例: "r8-2-teirei:議案第7号"）
 *   - 会期キーは sessions.json の key、議案番号ラベルは出典どおりの表記
 *   - キーが実在しない場合、build-csv 実行時に警告が出る
 * - **配列の順番がそのままトップページの表示順になる**（先頭が一番上）
 *
 * ## 変更を反映する手順（詳細は docs/20260716_1100_運用保守ガイド.md）
 * 1. この配列を編集する
 * 2. `pnpm --filter @mirai-gikai/seed exec tsx tagawa/build-csv.ts` でCSV再生成
 * 3. 本番envで `seed:csv` を実行して再シード
 * 4. `/api/revalidate` でキャッシュをパージ
 */
export const FEATURED_BILLS = [
  "r7-6-teirei:認定第1号", // 令和6年度一般会計決算（不認定）
  "r7-5-rinji:議案第37号", // 第三者調査委員会設置条例の制定
  "r8-2-teirei:議案第7号", // 令和8年度一般会計予算
  "r8-4-teirei:議案第42号", // 農業委員会委員の任命（否決）
];
